Before running tests, you must configure FIRE and build it.
It is recommended to use all compressors and lthreads
./configure --enable-zlib --enable-snappy --enable-lthreads --enable-tcmalloc --enable-zstd

box - massless box
doublebox - massless doublebox
v2 - non-planar two-loop planar diagram

All this configs are ran from makefile located in FIRE6 directory
make -f tests.makefile

This tests cover all mentioned configuration options
